# Python packages for Ansible modules and common tasks
# These packages will be automatically installed in the Semaphore container

# Core packages for common Ansible modules
requests>=2.25.0
urllib3>=1.26.0

# Cloud providers
boto3>=1.20.0        # AWS
# azure>=4.0.0         # Azure (deprecated, use azure-* packages)
azure-mgmt-compute>=13.0.0
azure-mgmt-network>=12.0.0
azure-mgmt-resource>=15.0.0
google-cloud>=0.34.0 # Google Cloud

# Infrastructure and automation
docker>=5.0.0        # Docker module support
kubernetes>=12.0.0   # Kubernetes module support
netaddr>=0.8.0       # Network address manipulation
jinja2>=3.0.0        # Template engine (usually included with Ansible)

# Database connectivity
pymongo>=3.11.0      # MongoDB
psycopg2-binary>=2.8.0  # PostgreSQL (pre-compiled binary)
mysql-connector-python>=8.0.0  # MySQL

# Monitoring and logging
prometheus-client>=0.11.0
elasticsearch>=7.0.0

# Encryption and security
cryptography>=3.4.0
paramiko>=2.7.0      # SSH connections

# Data processing
pyyaml>=5.4.0        # YAML processing (usually included with Ansible)
jsonschema>=3.2.0    # JSON schema validation

# Network automation
napalm>=3.2.0        # Network automation
ncclient>=0.6.0      # NETCONF client

# Optional: Uncomment if needed for specific use cases
# ansible-runner>=2.0.0     # For programmatic Ansible execution
# hvac>=0.11.0              # HashiCorp Vault client
# ldap3>=2.8.0              # LDAP operations